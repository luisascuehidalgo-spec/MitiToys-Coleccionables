name: Apply Enviopack reconciliation idempotency

on:
  push:
    branches:
      - fix/enviopack-ambiguous-shipment-timeout-20260908
    paths:
      - .github/workflows/apply-enviopack-reconcile-idempotency-temp.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/enviopack-ambiguous-shipment-timeout-20260908
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Apply reconciliation idempotency guard
        run: python3 .github/patch_enviopack_reconcile_idempotency.py
      - name: Validate full suite
        run: |
          node --check api/admin.js
          node --check lib/shipping.js
          npm install --ignore-scripts --no-audit --no-fund
          npm test
      - name: Remove temporary files and commit
        run: |
          rm .github/patch_enviopack_reconcile_idempotency.py
          rm .github/workflows/apply-enviopack-reconcile-idempotency-temp.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add api/admin.js test/shipment-timeout-reconciliation.test.js .github/patch_enviopack_reconcile_idempotency.py .github/workflows/apply-enviopack-reconcile-idempotency-temp.yml
          git commit -m "Make Enviopack shipment reconciliation idempotent"
          git push origin HEAD:fix/enviopack-ambiguous-shipment-timeout-20260908
