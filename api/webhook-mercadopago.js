const crypto=require('crypto');const{getDb}=require('../lib/db');function parseSignatureHeader(v){const p={};for(const x of String(v||'').split(',')){const[a,...b]=x.split('=');if(a&&b.length)p[a.trim()]=b.join('=').trim()}return p}function isValidSignature(req,id){const secret=process.env.MERCADOPAGO_WEBHOOK_SECRET;if(!secret)return false;const{ts,v1}=parseSignatureHeader(req.headers['x-signature']||'');const rid=req.headers['x-request-id']||'';if(!ts||!v1)return false;const manifest=`id:${String(id).toLowerCase()};request-id:${rid};ts:${ts};`;const expected=crypto.createHmac('sha256',secret).update(manifest).digest('hex');try{return crypto.timingSafeEqual(Buffer.from(expected),Buffer.from(v1))}catch(_){return false}}function statusToOrderStatus(s){if(s==='approved')return'approved';if(s==='cancelled'||s==='rejected')return'cancelled';if(s==='refunded'||s==='charged_back')return'refunded';return'pending'}
module.exports=async(req,res)=>{if(req.method!=='POST')return res.status(405).json({error:'Método no permitido'});try{const body=req.body||{};const dataId=body?.data?.id||req.query?.['data.id']||req.query?.id;const type=body?.type||req.query?.type;if(type!=='payment'&&body?.action!=='payment.created'&&body?.action!=='payment.updated')return res.status(200).json({received:true,ignored:true});if(!dataId)return res.status(400).json({error:'Falta data.id'});if(!isValidSignature(req,dataId))return res.status(401).json({error:'Firma de Webhook inválida o clave no configurada.'});if(body?.live_mode===false)return res.status(200).json({received:true,simulated:true});const token=process.env.MERCADOPAGO_ACCESS_TOKEN;if(!token)return res.status(500).json({error:'Falta configurar MERCADOPAGO_ACCESS_TOKEN.'});const paymentResponse=await fetch(`https://api.mercadopago.com/v1/payments/${encodeURIComponent(dataId)}`,{headers:{Authorization:`Bearer ${token}`}});const payment=await paymentResponse.json();if(!paymentResponse.ok){console.error('Error consultando pago en Mercado Pago:',payment);return res.status(502).json({error:'No se pudo consultar el pago.'});}if(process.env.DATABASE_URL&&payment.external_reference){const sql=getDb();const rows=await sql`SELECT id,status,customer_id,quantity,product_id FROM orders WHERE external_reference=${payment.external_reference} LIMIT 1`;if(rows.length){const order=rows[0];let customerId=order.customer_id;const payer=payment.payer||{};const email=String(payer.email||'').trim().toLowerCase();const first=String(payer.first_name||'').trim();const last=String(payer.last_name||'').trim();const name=([first,last].filter(Boolean).join(' ')||'Cliente Mercado Pago').slice(0,120);const phone=String(payer.phone?.number||'').trim().slice(0,50);if(!customerId&&email&&/^\S+@\S+\.\S+$/.test(email)){const cr=await sql`INSERT INTO customers(name,email,phone) VALUES(${name},${email},${phone||null}) ON CONFLICT(email) DO UPDATE SET name=EXCLUDED.name,phone=COALESCE(EXCLUDED.phone,customers.phone) RETURNING id`;customerId=cr[0].id;}const oldStatus=order.status;const newStatus=statusToOrderStatus(payment.status);await sql`UPDATE orders SET customer_id=${customerId},payment_id=${String(payment.id)},payment_status=${payment.status||null},payment_status_detail=${payment.status_detail||null},status=${newStatus},updated_at=NOW() WHERE id=${order.id}`;if(newStatus==='cancelled'||newStatus==='refunded'){const itemRows=await sql`SELECT product_id,quantity FROM order_items WHERE order_id=${order.id}`;const items=itemRows.length?itemRows:[{product_id:order.product_id,quantity:order.quantity}];for(const item of items){const reserved=await sql`SELECT id FROM inventory_movements WHERE order_id=${order.id} AND product_id=${item.product_id} AND movement_type='reserve' AND quantity<0 LIMIT 1`;const released=await sql`SELECT id FROM inventory_movements WHERE order_id=${order.id} AND product_id=${item.product_id} AND movement_type='release' AND quantity>0 LIMIT 1`;if(reserved.length&&!released.length){await sql`UPDATE products SET stock_quantity=stock_quantity+${item.quantity},updated_at=NOW() WHERE id=${item.product_id}`;await sql`INSERT INTO inventory_movements(product_id,order_id,movement_type,quantity,reason) VALUES(${item.product_id},${order.id},'release',${item.quantity},${newStatus==='refunded'?'Liberación por reembolso':'Liberación por rechazo/cancelación'})`}}}await sql`INSERT INTO order_events(order_id,event_type,old_status,new_status,payload) VALUES(${order.id},${body.action||'payment.notification'},${oldStatus},${newStatus},${JSON.stringify({payment_id:payment.id,status:payment.status,status_detail:payment.status_detail,transaction_amount:payment.transaction_amount})})`;}else console.warn('Webhook recibido sin pedido asociado:',payment.external_reference)}return res.status(200).json({received:true,payment_id:payment.id,status:payment.status})}catch(error){console.error('Webhook Mercado Pago error:',error);return res.status(500).json({error:'Error procesando el Webhook.'})}};
